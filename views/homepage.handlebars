<section class="posts">
    {{#each posts as |post|}}
    <div class="post">
        <h3>{{post.title}}</h3>
        <p>{{post.post_content}}</p>
        <a class="comment-button" href="/post/{{post.id}}">{{post.comments.length}} Comments</a>
    </div>
    {{/each}}
</section>